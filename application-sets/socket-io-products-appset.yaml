apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: socket-io-products
  namespace: argo-cd
  labels:
    app.kubernetes.io/part-of: kargo-gitops-demo
    app.kubernetes.io/component: socket-io
    app.kubernetes.io/managed-by: argocd
spec:
  generators:
    - list:
        elements:
          - env: dev
            server: https://dev-control-plane:6443
            namespace: socket-io
          - env: test
            server: https://test-control-plane:6443
            namespace: socket-io
          - env: prod
            server: https://prod-control-plane:6443
            namespace: socket-io
  template:
    metadata:
      name: socket-io-{{env}}
      labels:
        app.kubernetes.io/part-of: kargo-gitops-demo
        app.kubernetes.io/component: socket-io
        app.kubernetes.io/managed-by: argocd
        environment: '{{env}}'
      annotations:
        kargo.akuity.io/authorized-stage: 'socket-io:{{env}}'
    spec:
      project: default
      source:
        repoURL: https://github.com/iamimran96/kargo-gitops-demo.git
        targetRevision: HEAD
        path: products/{{env}}/socket-io
      destination:
        server: '{{server}}'
        namespace: '{{namespace}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
